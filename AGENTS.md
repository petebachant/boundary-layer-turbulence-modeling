# Agent instructions

This is a Calkit project to ensure all results are reproducible.
All output artifacts should be created by the pipeline.

Every experiment we do becomes a pipeline stage, so they all live at HEAD.

All commits should be atomic, subject-line-only, with no co-authors.

File paths should typically be kebab-case.
